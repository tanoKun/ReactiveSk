class Counter:
    field:
        factor count: number

    init:
        resolve count := 0
    
    function increment():
        notify that set count of [this] to count of [this] + 1 

command /counter_class:
    trigger:
        immutable counter := create Counter
        [counter] -> increment()

        send "%count of [counter]%"